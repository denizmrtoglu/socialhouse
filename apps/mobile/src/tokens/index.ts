/**
 * Mobile token re-export.
 * @repo/tokens'tan tüm token'ları alır ve RN'de kullanıma hazır hale getirir.
 */
export {
  neutral,
  accent,
  status,
  colors,
  darkColors,
  getSemanticColors,
  spacing,
  radius,
  shadows,
  fontSize,
  fontWeight,
  fontFamily,
  lineHeight,
  letterSpacing,
} from '@repo/tokens'

/** Kısayol — uygulama genelinde kullanılacak renk seti */
export { darkColors as theme } from '@repo/tokens'
